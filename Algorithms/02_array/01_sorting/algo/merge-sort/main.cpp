#include <bits/stdc++.h>
using namespace std;

void mergeSort(vector<int>& arr) {}

void printArr(vector<int> arr) {
    // Printing all elements of array
    for (const int& n : arr)
        cout << n << " ";
    cout << endl;
}

int main() {
    return 0;
}